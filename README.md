# Coati - Система управления требованиями

Полнофункциональная система сбора и анализа требований на базе AI с открытым исходным кодом.

## Архитектура

Проект состоит из следующих компонентов:

- **Frontend** (`frontend/`) - React приложение на TypeScript
- **Backend** (`infra/`) - n8n автоматизации workflows
- **Database** - ArangoDB NoSQL база данных
- **Infrastructure** - Docker контейнера для развертывания

## Технологии

- **Frontend**: React, TypeScript, Vite, Tailwind CSS
- **Backend API**: n8n (low-code automation)
- **Database**: ArangoDB Foxx Apps (JavaScript on ArangoDB)
- **Infrastructure**: Docker & Docker Compose
- **AI** Models: OpenAI GPT, Gemini, локальные модели
- **API Communication**: RESTful webhook между frontend и n8n

## Быстрый запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/yourusername/coati.git
   cd coati
   ```

2. **Запустите всю инфраструктуру:**
   ```bash
   docker-compose up -d
   ```

3. **Запустите frontend разработку:**
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

4. **Доступ к приложениям:**
   - **Frontend**: http://localhost:5173
   - **n8n API**: http://localhost:5678
   - **ArangoDB WebUI**: http://localhost:8529 (_system/database)
   - **n8n WebUI**: http://localhost:5678 (для управления workflows)

## Структура проекта

```
coati/
├── docker-compose.yml        # Основная инфраструктура
├── frontend/                 # React приложение
│   ├── src/
│   │   ├── components/       # UI компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── api.ts           # API клиент
│   │   ├── lib/apiLogger.ts # Система отладки API
│   │   └── ...
│   ├── package.json
│   └── README.md
├── infra/
│   ├── n8n_data/            # n8n workflows и настройки
│   ├── arangodb_apps/
│   │   ├── projects/        # Foxx приложения для проектов
│   │   ├── qa/              # Q&A управление
│   │   └── requirements/    # Требования анализ
│   └── init-scripts/        # Скрипты инициализации
├── scripts/
│   ├── init-database.js     # Создание коллекций и индексов
│   └── seed-database.js     # Начальные тестовые данные
├── docs/                    # Спецификации и документация
├── PROJECT_FULL_SPEC.md     # Полная спецификация проекта
├── QnA_UX_UI_Requirements.md # Требования к UX/UI
├── billing_extracted.json   # Примеры данных биллинга
└── README.md                # Этот файл
```

## Основные возможности

### 📋 Управление проектами
- Создание и управление проектами требований
- Многоэтапные рабочие процессы
- Версионирование и история изменений

### 🤖 AI анализ требований
- Автоматическое извлечение требований из текста
- Анализ качества и полноты требований
- Генерация недостающих требований
- Определение зависимостей и связей

### 🎯 Q&A система
- Интеллектуальные ответы на вопросы о требованиях
- Автоматическая генерация Q&A по документам
- Какие-навыки ответов по контексту

### 🔍 Отладка и мониторинг
- Живой лог API запросов с деталями
- Трассировка запросов от компонентов до базы
- Фильтры по статусу и действиям
- Экспорт логов для анализа

### 🗃️ Данные управления
- ArangoDB документы для гибкого хранения
- Гр эффективности ссылок между объектами
- Полнотекстовый поиск по требованиям

## Разработка

### Настройка окружения

1. **Prerequisites:**
   - Docker & Docker Compose
   - Node.js 18+ (для frontend разработки)
   - Git

2. **Clone the repository:**
   ```bash
   git clone https://github.com/yourusername/coati.git
   cd coati
   ```

3. **Setup infrastructure:**
   ```bash
   # Запуск базовых сервисов
   docker-compose up arangodb n8n --force-recreate

   # Инициализация базы данных
   docker compose exec arangodb arangosh --server.database _system --javascript.execute /scripts/init-database.js

   # Развертывание Foxx приложений
   curl -X POST -u root: "http://localhost:8529/_api/foxx/install?mount=/projects" \
        -d "@/infra/arangodb_apps/projects.zip" \
        -H "Accept: application/json"
   ```

4. **Frontend development:**
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

### Рабочий процесс разработки

1. **Frontend изменения:** `frontend/` - стандартный React + Vite workflow
2. **n8n workflows:** Редактировать в web UI, экспортировать JSON в `infra/n8n_data/`
3. **ArangoDB схемы:** Изменения в Foxx apps, деплоить через REST API
4. **Database migrations:** Scripts в `scripts/` выполняются при изменении схемы

## Deployment

### Production сборка

```bash
# Frontend build
cd frontend
npm run build

# Serve built files
npm run preview  # Для тестирования
# Или использовать веб-сервер
```

### Docker production

```bash
# Build и запуск всего стека
docker-compose -f docker-compose.prod.yml up --build
```

## Контрибьютинг

1. Fork the repository
2. Create feature branch (`git checkout -b feature/amazing-feature`)
3. Commit changes (`git commit -m 'Add amazing feature'`)
4. Push to branch (`git push origin feature/amazing-feature`)
5. Open Pull Request

## Лицензия

This project is licensed under the MIT License - see the LICENSE file for details.

## Автор

Built by [Your Name] | [Москва, 2025]

---

*"Требования - это язык, на котором разговаривает бизнес со разработкой"* 😉
