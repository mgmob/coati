# MCP-Arango: Примеры использования

> **MCP-Arango** - это инструмент для безопасной работы с ArangoDB прямо из Cline-чата.

## 🎯 Быстрый старт

После установки MCP-сервера (см. [backend/mcp-arango/README.md](../backend/mcp-arango/README.md)), вы можете использовать его в диалоге с Cline простыми командами на естественном языке.

## 📚 Примеры команд

### Просмотр данных

**Показать все коллекции:**
```
User: "Покажи все коллекции в базе"
```

**Получить статистику:**
```
User: "Покажи статистику базы данных"
```

**Выполнить запрос:**
```
User: "Покажи все атомы с статусом active"
User: "Найди все sections с title содержащим 'Аутентификация'"
User: "Покажи последние 10 созданных artifacts"
```

### Создание индексов

**Создать простой индекс:**
```
User: "Создай индекс на поле status в коллекции atoms"
```

**Создать составной индекс:**
```
User: "Создай индекс на поля locked_by и locked_until в коллекции atoms"
```

**Создать sparse индекс:**
```
User: "Создай sparse индекс на поле locked_by в коллекции atoms"
```

### Валидация графа

**Проверить целостность:**
```
User: "Проверь целостность графа"
User: "Запусти validation script"
User: "Есть ли проблемы в структуре графа?"
```

### Работа с запросами

**Сложные AQL запросы:**
```
User: "Покажи все атомы, у которых есть открытые issues"

User: "Найди все proposals, которые указывают на archived атомы"

User: "Покажи статистику по типам artifacts"
```

## 🔍 Сценарии использования

### Сценарий 1: Проверка перед миграцией

```
User: "Я хочу подготовиться к миграции v2.0 → v2.1"

Cline: Давайте проверим текущее состояние:

1. User: "Покажи статистику БД"
   → Проверяем общее состояние

2. User: "Проверь целостность графа"
   → Запускаем validation

3. User: "Есть ли sections с parent_doc_id?"
   → Cline выполнит:
   FOR s IN sections FILTER s.parent_doc_id != null RETURN s

4. User: "Покажи все прямые связи от docs к atoms"
   → Проверяем illegal links
```

### Сценарий 2: Отладка проблем

```
User: "У меня проблемы с proposals"

Cline: Давайте проверим:

1. User: "Покажи все proposals"
   → Выводим список

2. User: "Есть ли proposals, указывающие на неактивные атомы?"
   → Cline найдет некорректные связи

3. User: "Проверь proposal_links на корректность"
   → Запустит специфичную проверку
```

### Сценарий 3: Анализ данных

```
User: "Хочу проанализировать использование AI в проекте"

Cline:

1. User: "Сколько у нас artifacts типа ai_error?"
   → FOR a IN artifacts FILTER a.type == 'ai_error' COLLECT WITH COUNT INTO count RETURN count

2. User: "Покажи распределение severity у issues"
   → GROUP BY запрос

3. User: "Какие промпты чаще всего падают?"
   → JOIN artifacts с created_with
```

### Сценарий 4: Подготовка к релизу

```
User: "Подготовимся к созданию baseline"

Cline:

1. User: "Проверь целостность графа"
   → Валидация перед релизом

2. User: "Сколько у нас active атомов?"
   → Подсчет

3. User: "Есть ли orphaned nodes?"
   → Специфичная проверка

4. User: "Все ли индексы созданы?"
   → Проверка индексов
```

## ⚡ Полезные AQL запросы

### Статистика по статусам атомов
```aql
FOR atom IN atoms
  COLLECT status = atom.status WITH COUNT INTO count
  RETURN { status, count }
```

### Найти атомы с множественными родителями
```aql
FOR atom IN atoms
  LET incoming = (
    FOR v, e IN INBOUND atom structure_links
      FILTER e.type == "contains"
      RETURN 1
  )
  FILTER LENGTH(incoming) > 1
  RETURN {
    atom_id: atom._key,
    parents_count: LENGTH(incoming)
  }
```

### Проверить связи proposals
```aql
FOR edge IN proposal_links
  LET source = DOCUMENT(edge._from)
  LET target = DOCUMENT(edge._to)
  FILTER source.status != "proposal" OR target.status != "active"
  RETURN {
    from: edge._from,
    from_status: source.status,
    to: edge._to,
    to_status: target.status
  }
```

### Найти все ошибки AI за последний день
```aql
FOR artifact IN artifacts
  FILTER artifact.type == "ai_error"
  FILTER DATE_TIMESTAMP(artifact.created_at) > DATE_NOW() - 86400000
  RETURN artifact
```

## 🎓 Советы по использованию

### 1. Всегда проверяйте перед изменениями

```
User: "Перед тем как что-то менять, проверь целостность графа"
```

### 2. Используйте dry-run для миграций

```
User: "Покажи, что изменится, если удалить parent_doc_id, но не выполняй"
```

### 3. Логируйте важные операции

```
User: "После создания индекса запиши это в лог миграций"
```

### 4. Проверяйте результаты

```
User: "После каждого изменения запускай validation"
```

## 🚨 Важные предупреждения

### ⚠️ Не удаляйте данные напрямую

MCP-Arango предназначен для **просмотра** и **анализа** данных. Для изменений используйте миграционные скрипты через отдельную фазу 1C-Migration.

### ⚠️ Проверяйте запросы

Перед выполнением сложных AQL запросов всегда проверяйте синтаксис и логику.

### ⚠️ Делайте бэкапы

Перед любыми структурными изменениями создавайте backup БД:
```bash
npm run db:backup
```

## 📖 Дополнительные ресурсы

- [Техническая документация MCP-сервера](../backend/mcp-arango/README.md)
- [Architecture Specification v2.1](progress/20251217_architecture-v2.1-section1.md)
- [План миграции v2.0 → v2.1](progress/plan/07a-detailed-implementation-checklist-phase-1C-migration.md)
- [Validation Checklist](progress/20251217_architecture-v2.1-section4.md)

## 🤔 Вопросы и ответы

**Q: Как узнать, подключен ли MCP-сервер?**
A: Просто спроси Cline: "Покажи список коллекций". Если получишь ответ - все работает!

**Q: Можно ли выполнять INSERT/UPDATE/DELETE через MCP?**
A: Технически да (через `arango_query`), но **не рекомендуется**. Используйте миграционные скрипты для безопасности.

**Q: Что делать, если validation нашел проблемы?**
A: Изучи детали проблем, создай issue в проекте, подготовь миграционный скрипт для исправления.

**Q: Как часто запускать validation?**
A: Рекомендуется запускать:
- Перед каждым релизом
- После структурных изменений в БД
- При подозрении на проблемы с данными

---

**Боже в помощь мою вонми, Господи помощи ми потщися** 🙏
