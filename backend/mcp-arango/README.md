# MCP-Arango Server

MCP (Model Context Protocol) сервер для управления ArangoDB в проекте Coati.

## 📋 Описание

Этот MCP-сервер предоставляет набор инструментов для безопасной работы с графовой базой данных ArangoDB прямо из Cline. Сервер реализует 5 основных операций для управления БД, выполнения запросов и валидации целостности графа согласно Architecture Specification v2.1.

## 🚀 Установка

### 1. Установить зависимости

```bash
cd backend/mcp-arango
npm install
```

### 2. Настроить переменные окружения

Переменные настраиваются в файле `.cline_mcp_settings.json` в корне проекта:

```json
{
    "mcpServers": {
        "arango": {
            "command": "node",
            "args": ["backend/mcp-arango/index.js"],
            "env": {
                "ARANGO_URL": "http://localhost:8529",
                "ARANGO_DB": "coati_dev",
                "ARANGO_USERNAME": "root",
                "ARANGO_PASSWORD": ""
            }
        }
    }
}
```

### 3. Перезапустить Cline

После создания конфигурационного файла нужно перезапустить Cline (VS Code), чтобы MCP-сервер был подключен.

## 🛠️ Доступные инструменты

### 1. `arango_query`

Выполняет произвольный AQL запрос.

**Параметры:**
- `aql` (string, required) - AQL запрос
- `bindVars` (object, optional) - Параметры запроса

**Пример:**
```
User: "Покажи все атомы с статусом active"
Cline → arango_query({
  aql: "FOR atom IN atoms FILTER atom.status == 'active' RETURN atom",
  bindVars: {}
})
```

### 2. `arango_create_index`

Создает индекс на коллекции. Проверяет, не существует ли индекс уже.

**Параметры:**
- `collection` (string, required) - Имя коллекции
- `fields` (array, required) - Массив полей для индекса
- `type` (string, optional) - Тип индекса: 'persistent', 'fulltext', 'unique' (default: 'persistent')
- `sparse` (boolean, optional) - Sparse index (default: false)

**Пример:**
```
User: "Создай индекс на atoms.status"
Cline → arango_create_index({
  collection: "atoms",
  fields: ["status"],
  type: "persistent"
})
```

### 3. `arango_list_collections`

Получает список всех коллекций с количеством документов.

**Параметры:** нет

**Пример:**
```
User: "Покажи все коллекции в базе"
Cline → arango_list_collections({})
```

**Ответ:**
```json
{
  "success": true,
  "totalCollections": 14,
  "collections": [
    { "name": "atoms", "type": "document", "count": 150 },
    { "name": "structure_links", "type": "edge", "count": 200 }
  ]
}
```

### 4. `arango_validate_graph`

Запускает комплексную валидацию целостности графа согласно Architecture Spec v2.1.

**Проверки:**
- Single Parent Invariant (атомы имеют ≤1 родителя)
- Orphaned nodes (активные атомы без связей)
- Illegal `docs -> atoms` связи
- Циклы в `revision_links`
- Корректность `proposal_links`
- Соответствие схеме v2.1 (отсутствие `parent_doc_id`)

**Параметры:** нет

**Пример:**
```
User: "Проверь целостность графа"
Cline → arango_validate_graph({})
```

**Ответ (успех):**
```json
{
  "success": true,
  "ok": true,
  "timestamp": "2025-12-17T21:00:00.000Z",
  "totalIssues": 0,
  "categoriesWithIssues": 0,
  "issues": []
}
```

**Ответ (с проблемами):**
```json
{
  "success": false,
  "ok": false,
  "timestamp": "2025-12-17T21:00:00.000Z",
  "totalIssues": 3,
  "categoriesWithIssues": 2,
  "issues": [
    {
      "category": "Single Parent Invariant",
      "count": 2,
      "severity": "critical",
      "items": [...]
    }
  ]
}
```

### 5. `arango_get_stats`

Получает статистику базы данных.

**Параметры:** нет

**Пример:**
```
User: "Покажи статистику БД"
Cline → arango_get_stats({})
```

**Ответ:**
```json
{
  "success": true,
  "totalCollections": 14,
  "documentCollections": 8,
  "edgeCollections": 6,
  "totalDocuments": 2500,
  "collections": [...]
}
```

## 📁 Структура проекта

```
backend/mcp-arango/
├── index.js              # Главный файл MCP сервера
├── package.json          # Зависимости
├── lib/
│   ├── database.js       # Обертка для ArangoDB подключения
│   ├── tools.js          # Реализация 5 MCP tools
│   └── validators.js     # Логика валидации графа
└── README.md            # Эта документация
```

## 🔒 Безопасность

- **Санитизация запросов**: Все AQL запросы используют bind variables для защиты от инъекций
- **Проверка подключения**: Автоматическое переподключение при потере связи
- **Graceful shutdown**: Корректное закрытие соединения при остановке сервера
- **Error handling**: Все ошибки перехватываются и возвращаются в читаемом формате

## 🐛 Отладка

### Логи сервера

MCP-сервер выводит логи в stdout:
- `✅` - Успешные операции
- `❌` - Ошибки
- `⚠️` - Предупреждения
- `🔍` - Валидация
- `🚀` - Старт сервера

### Проверка подключения

```bash
# Проверить, запущен ли ArangoDB
curl http://localhost:8529/_api/version

# Проверить, доступна ли база данных
curl http://localhost:8529/_db/coati_dev/_api/version
```

### Типичные проблемы

**Проблема:** "Database not connected"
**Решение:** Убедитесь, что ArangoDB запущен на `localhost:8529`

**Проблема:** "Authentication failed"
**Решение:** Проверьте `ARANGO_USERNAME` и `ARANGO_PASSWORD` в `.cline_mcp_settings.json`

**Проблема:** MCP-сервер не появляется в Cline
**Решение:**
1. Проверьте синтаксис `.cline_mcp_settings.json`
2. Перезапустите VS Code
3. Проверьте, что зависимости установлены: `npm install`

## 📚 Дополнительная документация

- [Примеры использования](../../docs/mcp-arango.md)
- [Architecture Specification v2.1](../../docs/progress/20251217_architecture-v2.1-section1.md)
- [Validation Checklist](../../docs/progress/20251217_architecture-v2.1-section4.md)

## 🧪 Тестирование

### Запуск тестов

```bash
npm test           # Единичный прогон
npm run test:watch # Watch mode
```

### Покрытие тестами

**Текущее покрытие:**
- ✅ `lib/tools.js` - 16 unit-тестов
  - handleArangoQuery (4 теста)
  - handleCreateIndex (5 тестов)
  - handleListCollections (3 теста)
  - handleGetStats (2 теста)
  - handleValidateGraph (2 теста)

**Что НЕ тестируем (согласно .clinerules):**
- ❌ `index.js` - MCP server setup (инфраструктурный код)
- ❌ Реальные подключения к ArangoDB (используем моки)

### Результаты тестов

```
Test Files  1 passed (1)
     Tests  16 passed (16)
  Duration  551ms
```

## 🤝 Вклад

При добавлении новых tools:
1. Добавить tool definition в `lib/tools.js`
2. Добавить handler function в `lib/tools.js`
3. Обновить `handleToolCall` switch
4. **Написать unit-тесты** в `__tests__/tools.test.js`
5. Запустить `npm test` для проверки
6. Обновить документацию

## 📝 Лицензия

MIT
